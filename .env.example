# Copy to .env. Everything here is optional — ingest must never block on it.
#
# FantasyPros consensus ECR/ADP. The free tier is prototype-only; when this is
# unset the ingest skips the source cleanly and the board falls back to the
# Sleeper public-draft ADP proxy (and says so in its assumptions block).
FANTASYPROS_API_KEY=
